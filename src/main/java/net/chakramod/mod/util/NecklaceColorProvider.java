/*package net.chakramod.mod.util;

import net.fabricmc.api.EnvType;
import net.fabricmc.api.Environment;
import net.fabricmc.fabric.api.client.rendering.v1.ColorProviderRegistry;
import net.minecraft.item.ItemStack;
import net.minecraft.util.registry.Registry;


@Environment(EnvType.CLIENT)
public class NecklaceColorProvider {
    ColorProviderRegistry.ITEM.register((stack, tintIndex) -> 0x3495eb, COLORED_ITEM);

    public static void colorProvider(int color){

    }

    private static register(ItemStack stack){
        return ColorProviderRegistry.ITEM.register((stack1, tintIndex) -> -1, );
    }
}*/
